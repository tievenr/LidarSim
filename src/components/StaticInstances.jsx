import React from 'react';

const StaticInstances = () =>
{
    // Placeholder: Replace with InstancedMesh2 groups in the next steps
    return (
        <>
            {/* Static objects will go here (distance markers, buildings, trees, etc.) */}
        </>
    );
};

export default StaticInstances;